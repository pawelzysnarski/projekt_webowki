FROM node:22-alpine

WORKDIR /app

RUN apk add --no-cache mariadb-client bash

COPY package*.json ./
COPY prisma ./prisma/

RUN npm install && npm cache clean --force && npx prisma generate

COPY . .

EXPOSE 3000

CMD sh -c "sleep 5 && npx tsx src/index.ts"